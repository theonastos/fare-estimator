package main

import (
	"testing"
)

func TestAppendToFile(t *testing.T) {
	// testWriteChannel := make(chan []string)

	// records := [][]string{
	// 	{"1", "3.47"},
	// 	{"2", "5.6"},
	// 	{"3", "7.39"}}

	// outputFile, err := os.OpenFile("output.csv", os.O_CREATE, 0666)
	// if err != nil {
	// 	panic(err)
	// }

	// w := csv.NewWriter(outputFile)

	// testWriteChannel <- records[0]
	// testWriteChannel <- records[1]
	// testWriteChannel <- records[2]

	// appendToFile(w, testWriteChannel)

	// close(testWriteChannel)
}
